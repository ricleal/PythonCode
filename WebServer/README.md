In this folder implement a webserver to handle HTTP requests and serve web content.
- It only should handle HTTP GET requests for now
- Use `concurrent.futures` (may be `ThreadPoolExecutor`) to handle multiple requests concurrently
- Serve static files from a specified directory

    